[package]
name = "git_master"
version = "0.1.0"
edition = "2024"

[features]
test-rpc = ["dep:serde", "dep:serde_json"]

[dependencies]
gpui = "0.2.2"
git2 = "0.21.0"
chrono = "0.4"
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", optional = true }
